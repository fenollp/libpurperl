{erl_opts, [debug_info
           ]}.

{deps, []}.

{plugins, [pc]}.

{provider_hooks, [{pre, [{compile, {pc, compile}}
                        ,{clean, {pc, clean}}
                        ]}
                 ]}.
